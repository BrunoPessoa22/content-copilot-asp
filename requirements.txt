# OKX x402 seller SDK (X Layer / eip155:196). Requires Python >= 3.11.
# PINNED: this is an alpha SDK and a silent change to its NETWORK_CONFIGS asset
# name would re-break all signatures (see app/main.py override). Bump deliberately.
okxweb3-app-x402[fastapi,evm,httpx]==0.1.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
# Pipeline
anthropic>=0.40.0
yt-dlp>=2025.6.9
faster-whisper>=1.0.0
# Local MCP (stdio) server
mcp>=1.0.0
